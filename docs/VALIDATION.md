# 検証記録 — Version 5.0.0

検査日: 2026-09-15

## 実施した検査

| 検査 | 結果・証拠 |
|---|---|
| 公開HTML | 80ページ |
| ローカルファイル・アンカー参照 | 6,442件、参照切れ0件 |
| 重複ID・目次と見出しの対応 | 問題0件 |
| 公開用ファイルのローカルHTTP配信 | 379ファイル、HTTP 200とファイル内容の一致 |
| 演習・確認問題の維持 | 96演習・160問 |
| 追加図解 | 32SVG、64掲載箇所。SVGのXMLとテキスト境界を検査 |
| 追加実例 | 64本、64掲載箇所 |
| コード表示 | 717ブロック |
| インメモリDOM表示・操作、図版・コードの静的検査 | 213項目中213項目PASS |
| 表示幅 | 1536px・1024px・390px。代表14ページ×3幅 |

PythonのHTTPサーバーとurllibを使用した配信検査は `validation/visual/local-http.json` です。これはブラウザーの公開URL遷移やC# Web APIの検査ではありません。

構造検査は `validation/visual/structural-audit.json`、表示・操作等の明細は `validation/visual/browser-audit.json` です。JavaScriptの構文検査は `validation/visual/javascript-syntax.json`、再生成の一致は `validation/visual/reproducibility.json` に記録します。

確認した操作には、現在Chapterの開閉、左側目次の見出し移動、本文を動かさない右用語欄の開閉、モバイル目次、Escapeで閉じる操作、下書きの保存ロジック、確認問題の採点、完了・復習マーク、.csファイルのダウンロード内容、検索、コードの折り返し、ダークモードが含まれます。確認問題はDOM上で正解選択状態を設定して採点を実行しており、全ラジオボタンを実機で手動操作した検査ではありません。

## 検査環境と限界

Chromiumの通常のURL直接移動は管理ポリシーにより拒否されました。そのため、生成したHTMLへ同梱CSS・JavaScript・SVGをインライン化して読み込む表示検査を実施しました。コード本体は配信時のものを使用していますが、テストの保存領域はメモリ上の代替です。

したがって、以下は未検証です。

- 公開URLの通常ナビゲーションと実オリジンでの永続保存。
- Safari/iPhone/iPad実機、ネイティブの200%ズーム。
- 300件のC#プロジェクトのコンパイル・実行と、WebサンプルのHTTP応答。
- Cloudflare Pages / GitHub Pagesへの実際のデプロイ。

64本の新規コードには文字列・コメント・括弧などの字句的な静的検査を行いました。これはC#コンパイラーの型検査でも、実行時の動作検証でもありません。`expected.txt` と画面の「想定出力」は、観測済みの実行結果ではありません。

ブラウザー内C#ランナーは未実装です。表示確認のPASSを、C#実行機能が搭載済みである根拠として扱わないでください。

## 過去版の検証

`docs/archive/v4/` に入力版の記録を保管しています。過去のHTTP検査、コード比較数、スクリーンショットを今回の実績に加算していません。
