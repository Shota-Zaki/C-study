# C-study

## Start
Use work as the development source of truth. Read docs/rules/RULES_SOURCE.md, then docs/project/TASKS.md, NEXT_WORK.md and AI_WORK_STATE.md. /continue Shota-Zaki/C-study resumes from these files. Read only design/code relevant to the current task.

## Product
Japanese C#-only course from zero through .NET/CLR, types, OOP, LINQ, async/await and ASP.NET Core. Preserve 32 lessons, 160 questions with every choice explained, 96 exercises, worked examples, repairs and three projects. Explain concepts and causal mechanisms without prerequisites in another language. Never shorten useful content to fit the layout.

## Branches
work retains source, tests, design, state, evidence and rules. main retains only current publication/build/runtime files and the public README. Promote an allowlist, not the whole work tree. The user explicitly authorized initial verified promotion and static publication.

## Quality
Pre-render full lessons so reading does not require scripts. Test all lesson links, search, drafts, progress, quizzes, JSON import/export, light/dark, responsive widths and keyboard operation. Browser textareas are drafts, not a C# runtime or automatic grader. Compile samples when possible and report real results. Validate imported JSON, protect existing data on failure and escape text.

## Work units
Update canonical state in docs/project with code/tests. Mark only the affected task blocked. Leave the repository resumable without chat history.
