# 次のWork Unit

教材拡充・静的サイト生成・実施可能な監査は完了しています。引継ぎ時は `README.md` → `docs/VALIDATION.md` → `docs/work/state.json` の順で確認してください。

## SDKでの検証

`python scripts/verify_dotnet.py --build` を実施し、236の独立プロジェクトの診断を記録します。失敗があれば元のMarkdown / 教材データを修正して再生成し、想定出力と処理順・状態表の整合性も確認します。失敗例・部分例・テスト例を独立コンソールとして起動しません。

## ブラウザーE2E

URL移動が許可された環境で、同梱サイトをHTTP配信して閲覧します。ページ遷移、同一オリジンでの保存→再読込、書き出し→別環境での読込、実機モバイル、キーボード、拡大を確認します。今回のDOM代替検査と実E2Eを別の記録にしてください。

## 教材運用

初学者に一章ずつ使ってもらい、説明を言い換えられない箇所、コードを変更すると止まる箇所を記録します。ページ数・文字数を上限にせず、必要な具体例・失敗と修正を追加します。

リポジトリ操作は不要です。成果物はZIPで出力します。
